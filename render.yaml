services:
  - type: web
    name: yve01-dashboard
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python dashboard.py
    envVars:
      - key: ANTHROPIC_API_KEY
        sync: false